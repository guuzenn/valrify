import type { Metadata } from "next";import Link from "next/link";import { notFound } from "next/navigation";import { Header,Footer,Disclaimer } from "../../components/SiteChrome";import { formatRupiah,labelIdentifierType } from "@vlrfy/domain";import { getPublicCase } from "../../../lib/api";
export const dynamic="force-dynamic";
export async function generateMetadata({params}:{params:Promise<{publicId:string}>}):Promise<Metadata>{const{publicId}=await params;const r=await getPublicCase(publicId);if(!r)return{title:'Laporan tidak ditemukan'};const description=String(r.publicSummary);return{title:`Case ${r.publicId}`,description,openGraph:{title:`Case ${r.publicId} — VLRFY`,description,images:[]},twitter:{card:'summary',title:`Case ${r.publicId} — VLRFY`,description,images:[]}}}
export default async function CasePage({params}:{params:Promise<{publicId:string}>}){const{publicId}=await params;const r=await getPublicCase(publicId);if(!r)notFound();return <><Header compact/><main className="page shell"><div className="case-heading"><div><p className="eyebrow">LAPORAN // {String(r.publicId)}</p><h1 className="page-title">{String(r.title).toUpperCase()}</h1></div><span className="status-badge">SUDAH DICEK ADMIN</span></div><section className="case-layout"><article className="case-body"><p className="panel-index">CERITA SINGKAT</p><p className="summary">{String(r.publicSummary)}</p><p className="source-line">DIKIRIM PENGGUNA · NAMA PELAPOR DISEMBUNYIKAN</p></article><aside className="case-facts"><div><span>STATUS</span><strong>TAYANG</strong></div><div><span>TANGGAL KEJADIAN</span><strong>{String(r.transactionDate||'Tidak dicantumkan')}</strong></div><div><span>UANG YANG DILAPORKAN HILANG</span><strong>{formatRupiah(Number(r.allegedLoss))}</strong></div><div><span>AKUN TERKAIT</span><Link href={`/entity/${r.slug}`}>{String(r.entityName)} ↗</Link></div></aside></section><section className="data-panel case-identifiers"><h2>NOMOR / AKUN TERKAIT</h2>{(r.identifiers as Array<{type:string;maskedValue:string}>).map((id,i)=><div className="identifier-row" key={i}><span>{labelIdentifierType(id.type)}</span><strong>{id.maskedValue}</strong></div>)}</section><Disclaimer/></main><Footer/></>}
