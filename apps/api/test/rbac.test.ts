import assert from "node:assert/strict";import test from "node:test";import {BadRequestException} from "@nestjs/common";import {canModerate,canTransition} from "@vlrfy/domain";import {confirmationReviewSchema,confirmationSchema} from "@vlrfy/validation";import {parseSchema} from "../src/validation/parse-schema";test("moderation roles and publish lifecycle",()=>{assert.equal(canModerate("USER"),false);assert.equal(canModerate("ADMIN"),true);assert.equal(canTransition("SUBMITTED","PUBLISHED"),false);assert.equal(canTransition("VERIFIED","PUBLISHED"),true)});test("successful transaction input is constrained",()=>{assert.equal(confirmationSchema.safeParse({entityId:1,transactionDate:"2026-08-20",amount:300000,note:"Transaksi demo berhasil."}).success,true);assert.equal(confirmationSchema.safeParse({entityId:1,transactionDate:"bad",amount:-1,note:"pendek"}).success,false)});test("schema errors become HTTP 400",()=>{assert.throws(()=>parseSchema(confirmationReviewSchema,{decision:"APPROVE",rationale:"x"}),(error)=>error instanceof BadRequestException&&error.getStatus()===400)});
