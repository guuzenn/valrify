import assert from "node:assert/strict";import test from "node:test";import {BadRequestException} from "@nestjs/common";import {canModerate,canTransition} from "@valrify/domain";import {confirmationReviewSchema,confirmationSchema,reportSchema} from "@valrify/validation";import {parseSchema} from "../src/validation/parse-schema";test("moderation roles and publish lifecycle",()=>{assert.equal(canModerate("USER"),false);assert.equal(canModerate("ADMIN"),true);assert.equal(canTransition("SUBMITTED","PUBLISHED"),false);assert.equal(canTransition("VERIFIED","PUBLISHED"),true)});test("successful transaction input is constrained",()=>{assert.equal(confirmationSchema.safeParse({entityId:1,transactionDate:"2026-08-20",amount:300000,note:"Transaksi demo berhasil."}).success,true);assert.equal(confirmationSchema.safeParse({entityId:1,transactionDate:"bad",amount:-1,note:"pendek"}).success,false)});test("reports accept multiple account identifiers",()=>{const result=reportSchema.safeParse({entityName:"Seller Demo",title:"Akun tidak dikirim",chronology:"Kronologi transaksi ini cukup panjang untuk memenuhi validasi laporan dan menjelaskan kejadian secara lengkap.",identifiers:JSON.stringify([{type:"PHONE",value:"081234567890"},{type:"BANK_ACCOUNT",value:"1234567890",provider:"BCA"}]),transactionDate:"2026-08-20",allegedLoss:"1000000",transactionType:"ACCOUNT_PURCHASE"});assert.equal(result.success,true);if(result.success)assert.equal(result.data.identifiers.length,2)});test("schema errors become HTTP 400",()=>{assert.throws(()=>parseSchema(confirmationReviewSchema,{decision:"APPROVE",rationale:"x"}),(error)=>error instanceof BadRequestException&&error.getStatus()===400)});
