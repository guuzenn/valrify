import { CanActivate,ExecutionContext,Injectable,UnauthorizedException } from "@nestjs/common";import { JwtService } from "@nestjs/jwt";import type { Request } from "express";import type { AuthActor } from "./auth.types";
export type AuthenticatedRequest=Request&{user:AuthActor};
@Injectable()export class JwtAuthGuard implements CanActivate{constructor(private readonly jwt:JwtService){}async canActivate(context:ExecutionContext){const request=context.switchToHttp().getRequest<AuthenticatedRequest>();const bearer=request.headers.authorization?.startsWith("Bearer ")?request.headers.authorization.slice(7):undefined;const token=request.cookies?.vlrfy_session??bearer;if(!token)throw new UnauthorizedException("Silakan masuk.");try{request.user=await this.jwt.verifyAsync<AuthActor>(token);return true;}catch{throw new UnauthorizedException("Sesi tidak valid.");}}}
