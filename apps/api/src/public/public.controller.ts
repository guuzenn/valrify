import { Controller,Get,NotFoundException,Param,Query } from "@nestjs/common";import { PublicService } from "./public.service";
@Controller()export class PublicController{constructor(private readonly service:PublicService){}@Get("search")search(@Query("q")query=""){return this.service.search(query)}@Get("entities/:slug")async entity(@Param("slug")slug:string){const result=await this.service.entity(slug);if(!result)throw new NotFoundException("Profil tidak ditemukan.");return result;}@Get("reports/public/:publicId")async report(@Param("publicId")publicId:string){const result=await this.service.publicCase(publicId);if(!result)throw new NotFoundException("Laporan tidak ditemukan.");return result;}}
