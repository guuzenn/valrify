import { redirect } from "next/navigation";import { chatGPTSignInPath } from "../chatgpt-auth";import { getActor } from "../../lib/auth";import { Header,Footer } from "../components/SiteChrome";import { SubmitReportForm } from "../components/SubmitReportForm";
export const dynamic="force-dynamic";
export default async function SubmitPage(){const actor=await getActor();if(!actor)redirect(chatGPTSignInPath('/submit'));return <><Header compact/><main className="page shell narrow"><p className="eyebrow">// KIRIM LAPORAN</p><h1 className="page-title">LAPORKAN DENGAN BUKTI.</h1><p className="page-intro">Laporan tidak langsung dipublikasikan. Moderator akan memeriksa evidence, menyamarkan data sensitif, dan memastikan ringkasan publik tetap netral.</p><div className="actor-bar">MASUK SEBAGAI <strong>{actor.displayName}</strong><span>{actor.role}</span></div><SubmitReportForm/></main><Footer/></>}
